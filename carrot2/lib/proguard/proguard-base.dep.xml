<?xml version="1.0" ?>
<!DOCTYPE component PUBLIC "-//Carrot2//DTD Component Dependency Descriptor V1.0//EN" "http://www.carrot2.org/dtd/component-dependency.dtd" >
<component
    name="proguard-base"
    description="Proguard, obfuscation base.">
	<provides>
		<meta type="proguard"><![CDATA[
# (proguard base) 

# Optimization may result in errors (ProGuard bugs), so we turn it off
-dontoptimize

# We need to preserve inner class names
-keepattributes InnerClasses

# Also keep Enumerations. Keep a method that is required in enumeration classes.
-keepclassmembers class * extends java.lang.Enum {
    public **[] values();
}

# Keep classes with native code.
-keepclasseswithmembernames class * {
    native <methods>;
}
		]]></meta>
	</provides>
</component>
