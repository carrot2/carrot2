
A roadmap to release 1.0
------------------------

- Add local interfaces to: 

carrot2-filter-ahc
carrot2-filter-fuzzyants
carrot2-filter-lingo
carrot2-filter-stc

- Add these clustering algorithms to the demo application:

carrot2-filter-ahc
carrot2-filter-fuzzyants
carrot2-filter-lingo
carrot2-filter-stc

- Remove obsolete/ unused components

- Alter all *.java files and add license statement to those originating
  from the project. Leave other licenses unchanged, clean up IDE junk.

- Unify coding style.

- Review documentation, ensure it corresponds to reality.

- Review remote component descriptors.

- Remove dependency on jdom and replace it with dom4j.

- Remove dependency on castor.

- Remove Google descriptors from snippet reader (and from the demo applications). Replace
them with Yahoo API (and GoogleAPI).

- Create a separate project (carrot2-util-local?) for utils related to local
  interfaces

- Review master ANT build file -- generate JavaDocs.

- Ensure all tests pass. Ensure the demo application works.

- Nightly deploy must include unit tests (currently only external tests are run).

- Public Relations
    - redirect www.carrot2.org to the O/S website
    - add new publications to the O/S website
