
<project name="Carrot2 Input Component using OpenSearch" default="build">

    <property name="tests.available"  value="false"    />

    <property name="project.dir"      location="${basedir}" />
    <property name="carrot2.cvs.dir"  location="../.." />
    <property name="dist.name"        value="carrot2-input-opensearch" />

    <import file="${carrot2.cvs.dir}/components/common-targets.inc" />

	<target name="build">
		<ant antfile="build.local.xml" inheritall="false"/>
	</target>

</project>
