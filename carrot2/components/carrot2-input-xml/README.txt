
An input component that can read an arbitrary XML stream
and transform it using the provided XSLT stylesheet.

Remote component usage:

Http request should contain 'xslt' and 'source' parameters which
are resolved as files relative to the component's Web application context
or files in the filesystem, accordingly.

