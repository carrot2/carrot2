
<project name="Carrot2 Polish Stemmer" default="build">

    <property name="project.dir"      location="${basedir}" />
    <property name="carrot2.code.dir"  location="../.." />

    <property name="dist.name" value="carrot2-stemmer-morfologik" />
    <property name="component.descriptor" location="carrot2-stemmer-morfologik.dep.xml" />

    <property name="type"       	  value="jartype" />
    <import file="${carrot2.code.dir}/components/common-targets.inc" />

</project>
