
<project name="c" default="build">
	<target name="clean">
		<delete dir="tmp" />
	</target>

	<target name="build">
		<mkdir dir="tmp/classes" />

		<javac srcdir="src" destdir="tmp/classes" debug="on" />

  		<jar destfile="tmp/c.jar" basedir="tmp/classes"/>
	</target>
</project>