
- Copy language resources during build. Run with explicit language resources.

- Split "client" classes and request/ response model.
  - Add examples of using the client to call the service.

- Add frontend subproject from Staszek.

- configure logging in the launcher (console, levels).

- add better error support (perhaps structured json response) and logging 
- add better validation of extra/ unused/ invalid json entries in parameters; handle response.

- TODOs and performance optimizations.
- documentation [another project/ dcs context?]
- Add an IT that verifies what happens if we saturate the server with requests.
