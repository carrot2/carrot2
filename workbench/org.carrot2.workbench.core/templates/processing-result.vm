
#parse("_header.vm")

#*
	Template displayed on show(ProcessingResult). 
	
	Available variables:

	  result
	  results-total-formatted
	  query
*#

#if ($result)
    #set ( $totalProcessingSec = ${result.Attributes.processing-time-total} / 1000.0 )
    #set ( $documents = $result.Documents )
#end

#if ($documents.isEmpty())
    Your query - <b>&quot;$esc.html($query)&quot;</b> - did not return any documents.<br><br> 

    Suggestions:<ul>
    <li>Make sure all words are spelled correctly.
    <li>Try different keywords.
    <li>Try more general keywords.
    </ul>
#else
	#foreach ($document in $documents)
	    <span class="id"> [$document.Id]</span>
	    <a href="$esc.html(${document.getField("url")})">
	    	<b>$esc.html($document.getField("title"))</b></a>
	    <br>
	    #if ($document.getField("summary") && $document.getField("summary") != "")
	        $esc.html($document.getField("summary"))<br>
	    #end
	    <span class="du">$esc.html(${document.getField("url")})</span><br><br>
	#end
#end

#parse("_footer.vm")
