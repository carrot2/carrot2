import com.carrotsearch.gradle.randomizedtesting.RandomizedTestingPlugin

allprojects {
  plugins.withType(RandomizedTestingPlugin) {
    randomizedtesting {
      failOnNoTests = true
      testOpts.maxHeap.defaultValue = "100m"
    }

    test {
      useJUnit()
    }
  }
}
