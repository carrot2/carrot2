{
  "private": true,
  "workspaces": [
    "app", "theme", "ui"
  ],
  "scripts": {
    "start": "yarn workspace app start",
    "build": "yarn workspace app build",
    "test": "yarn workspace app test",
    "prettier": "yarn workspace app prettier"
  }
}