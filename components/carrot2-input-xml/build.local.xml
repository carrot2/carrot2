
<project name="Carrot2 Input Component reading XML files and transforming them using XSLT (Local)" default="build">
    
    <property name="project.dir"      location="${basedir}" />
    <property name="carrot2.cvs.dir"  location="../.." />

    <property name="dist.name" value="carrot2-input-xml" />

    <property name="profile" value="local" />
    <property name="component.descriptor" location="carrot2-input-xml.dep.xml" />

    <import file="${carrot2.cvs.dir}/components/common-targets.inc" />

	<property name="type" value="jartype" />

    <patternset id="src.patternset">
        <exclude name="**/*Remote*.java" />
    </patternset>

</project>
