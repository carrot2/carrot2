
<project name="Carrot2 Stemming Component (for remote components architecture)" default="build">
    
    <property name="project.dir"      location="${basedir}" />
    <property name="carrot2.cvs.dir"  location="../.." />

    <property name="dist.name" value="carrot2-filter-stemmer" />

    <property name="component.descriptor" location="carrot2-filter-stemmer.dep.xml" />

    <import file="${carrot2.cvs.dir}/components/common-targets.inc" />

	<property name="type" value="wartype" />

</project>
