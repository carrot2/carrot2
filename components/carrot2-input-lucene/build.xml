
<project name="Carrot2 Input Component reading data from a Lucene index" default="build">

    <property name="tests.available"  value="false"    />

    <property name="project.dir"      location="${basedir}" />
    <property name="carrot2.code.dir"  location="../.." />
    <property name="dist.name"        value="carrot2-input-lucene" />

    <property name="type"       	  value="jartype" />
    <import file="${carrot2.code.dir}/components/common-targets.inc" />

</project>
