<!--
     A dependency descriptor. This has to be updated manually
     when new JARs are required.
   -->
<component name="lingo-clustering">
    <dependency name="carrot2-shared-lib" />
    <dependency name="jama" />
    <dependency name="lametyzator" />

    <!-- This could probably be replaced with jdk's -->
    <dependency name="gnu-regexp" />
    
    <!-- This could probably be removed -->
    <dependency name="colt" />
</component>
